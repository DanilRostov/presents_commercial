{
  "name": "presents_commercial",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "initDb": "node -r dotenv/config initDB.js",
    "start": "node -r dotenv/config server.js",
    "dev": "nodemon -r dotenv/config server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "Danil Rostov",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "mongoose": "^5.2.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.2"
  }
}
